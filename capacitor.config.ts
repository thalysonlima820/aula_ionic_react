import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.loginteste.novo',  // Identificador único
  appName: 'Login',
  webDir: 'dist'
};

export default config;
